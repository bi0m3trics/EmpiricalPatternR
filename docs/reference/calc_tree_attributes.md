# Calculate all tree attributes from basic measurements

Calculate all tree attributes from basic measurements

## Usage

``` r
calc_tree_attributes(trees)
```

## Arguments

- trees:

  Data table with x, y, Species, DBH columns

## Value

Data table with all attributes
